@model CustomerViewModel

<h1>@Model.CurrentCategory</h1>


@* @foreach (var Customer in Model.Customers)
{
    <div class="col">
        <div class="card pie-card">
           <img src="@Customer.imgeUrl" class="card-img-top img-fluid" alt="@Customer.Name" style="max-width: 300px;">

            <div class="card-body pie-button">
                <h4 class="d-grid">
                </h4>
                <div class="d-flex justify-content-between mt-2">
                    <div class="text-start">
                        <h2 class="pie-link">
                            @Customer.Name</h2>
                            <h2 class="pie-link">@Customer.Phone</h2>
                            <h2 class="pie-link">@Customer.favoriteCategory</h2>
                    
                   </div>
                </div>
            </div>
        </div>
    </div>
} *@
<div class="row row-cols-1 row-cols-md-3 g-4">
    @foreach (var Customer in Model.Customers)
    {
        <div class="col">
            <div class="card h-100">
                <img src="@Customer.imgeUrl" class="card-img-top" alt="@Customer.Name">
                <div class="card-body">
                    <h5 class="card-title">@Customer.Name</h5>
                    <p class="card-text">Phone: @Customer.Phone</p>
                    <p class="card-text">Favorite Category: @Customer.favoriteCategory</p>
                </div>
                <div class="card-footer">
                    <a href="#" class="btn btn-primary">View Details</a>
                </div>
            </div>
        </div>
    }
</div>